import React from 'react';

const MainPage = ({ username }) => {
    return (
        <div>
            <h2>Welcome, {username}!</h2>
            {/* 메인 페이지 내용 추가 */}
        </div>
    );
};

export default MainPage;
